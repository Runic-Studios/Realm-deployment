apiVersion: v1
kind: ConfigMap
metadata:
  name: paper-config
  namespace: {{ .Values.namespace }}
data:
  plugins-velagones-application.conf:
{{- range $path, $content := .Files.Glob "config/paper/**" }}
  {{- $key := $path | trimPrefix "config/paper/" | replace "/" "__" }}
  {{ $key }}: |-
{{ toString $content | indent 4 }}
{{- end }}
