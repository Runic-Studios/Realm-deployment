apiVersion: v1
kind: ConfigMap
metadata:
  name: velocity-base-config
  namespace: {{ .Values.namespace }}
data:
{{- range $path, $content := .Files.Glob "config/base/velocity/**" }}
  {{- $key := $path | trimPrefix "config/base/velocity/" | replace "/" "__" }}
  {{ $key }}: |-
{{ toString $content | indent 4 }}
{{- end }}
